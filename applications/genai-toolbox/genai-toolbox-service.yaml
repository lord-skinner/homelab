apiVersion: v1
kind: Service
metadata:
  name: genai-toolbox
  namespace: genai-toolbox
  labels:
    app: genai-toolbox
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: genai-toolbox
