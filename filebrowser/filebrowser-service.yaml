apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: filebrowser
spec:
  # ClusterIP only: external access goes through Traefik Ingress
  # (nas.home.datalab.gg). No LAN NodePort.
  type: ClusterIP
  selector:
    app: filebrowser
  ports:
    - port: 80
      targetPort: 80
