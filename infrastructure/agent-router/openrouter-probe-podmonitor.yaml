apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: openrouter-probe-exporter
  namespace: inference
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: openrouter-probe-exporter
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
