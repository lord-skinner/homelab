# Agent Router v1.1.0. The controller watches the model catalog and the
# namespaces served by the shared Gateway so its extension cache is coherent.
controller:
      watch:
        namespaces:
          - inference
          - kube-system
          - n8n
          - nextcloud
          - elastic-stack
          - docs-site
