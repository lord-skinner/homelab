apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: blinko

resources:
  - blinko-website-deployment.yaml
  - blinko-website-service.yaml
  - namespace.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
