# This script will provision an NFS server for hosting the root filesystem for machines booting over PXE.
# The server environment that NFS will be set up on is an ARM based device, but AMD devices will be booting from it.
